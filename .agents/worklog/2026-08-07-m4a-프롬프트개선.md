# 2026-08-07 M4a 프롬프트 개선

## 한 일

- 포맷 확인: 트레이스 관측치 56개(AGENT 9·SPAN 27·GENERATION 10·CHAIN 10). **GENERATION 의
  `input` 이 실제 프롬프트**(`[{role, content}]` messages). 대상 서버 생성 모델은 `gpt-5.4-mini`.
- `app/improve/`:
  - `schema.py`: `PromptSuggestion`(target_step·related_criteria·problem·suggestion) + `PromptReview`.
  - `suggest.py`: `collect_generation_prompts`(GENERATION 관측치→단계별 프롬프트, 같은 이름 dedup,
    메시지 3000자 절단) + `suggest_prompt_improvements`(문제점+프롬프트 → structured output).
- `app/main.py`: 채점 결과 아래 **"프롬프트 개선 제안"** 버튼 → 단계별 수정안 표.

## 결정과 근거

- 개선 대상은 **실제 프롬프트**(generation messages). 근거 없는 훈수를 막기 위해 판단은
  주어진 프롬프트 내용에만 기반하도록 프롬프트에 못박음(target_step 은 실제 단계명만).
- 프롬프트가 8단계(dedup 후)라 크지만 gpt-5.6-sol 컨텍스트로 충분. 메시지 3000자 절단으로 상한.
- judge 와 동일 클라이언트·모델·temperature 폴백 재사용.

## 검증

- 라이브: 프롬프트 8단계 수집 확인, 채점→개선제안 7건(단계·기준·문제·수정안 매핑) 정상.
  예: infer-location-events(temporal/place) 겹치는 STAY 처리 규칙 추가, analyze-timeline-repair
  sourceRefs↔시각 일치 검증.
- compile·import OK.

## 다음 할 일

- M4b 테스트 케이스 생성(입력 + 기대 조건).
- 로컬 커밋만(사용자 리뷰 후 푸시). PR 은 M4a·M4b 각각 별도.
