# 2026-08-07 M4b 테스트 케이스

## 한 일

- `app/testdata/`:
  - `schema.py`: `TestSuite`/`TestCase`(title·targets_criteria·description·input·expected) +
    `TestInput`(date·timezone·stays·movements·calendars·notifications)와 항목 모델.
  - `generate.py`: `generate_test_cases`(문제점 → structured output 스위트) + `testsuite_to_json`.
- `app/main.py`: 채점 결과 아래 **"테스트 케이스 생성"** 버튼 → 케이스별(기대조건·입력) 표시 + json 다운로드.

## 결정과 근거

- `input` 은 실제 수집 스냅샷의 **단순화 스키마**(시각·장소·제목 등 핵심 의미 필드만). rawId·좌표·
  transports 세부는 회귀/판정용 케이스엔 불필요 → 생략. strict structured output 이라 자유형 dict 대신
  구체 모델로 선언(모든 필드 required, nullable 로 optional 표현).
- `expected` 는 자연어 기대 조건('이런 event 가 나와야/나오면 안 됨'). judge·회귀 테스트 판정에 사용.
- 실명·실주소 금지(프롬프트에 명시), 가상 값만.

## 검증

- 합성 문제점(시간 겹침·장소 라벨) → 케이스 2건 생성. case1 targets temporal/coverage,
  기대조건 3개, input stays 2(겹치는 시각으로 문제 재현). json 내보내기 OK.
- compile·import OK.

## 남은 것 / 다음

- 로컬 커밋만(푸시 X) — 사용자 리뷰 후 M4a·M4b 각각 PR.
- 단순화 input 스키마는 대상 서버 실제 입력과 1:1 아님(회귀/판정 목적). 필요 시 후속에서 확장.
- 파이프라인 M0~M4 코드 완성. 라이브 GUI 전 구간(채점→리포트→개선→케이스) 확인은 선택.
