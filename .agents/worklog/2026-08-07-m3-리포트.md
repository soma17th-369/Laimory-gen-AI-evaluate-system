# 2026-08-07 M3 리포트

## 한 일

- `app/report/` 신설:
  - `schema.py`: `ReportItem`(메타 + scorecard), `ReportSummary`(집계), `Report`.
  - `build.py`: `build_report` — 기준별 평균·overall 평균·findings 심각도별 건수 계산, `generated_at` 스탬프.
  - `export.py`: `report_to_json`(ensure_ascii=False), `report_to_markdown`(요약 표 + 트레이스별 점수·문제점).
- `app/main.py`: 세션 채점 결과(`scorecards`)를 모아 **채점 리포트** 섹션 — 평균 점수 표 + `.md`/`.json` 다운로드 버튼.

## 결정과 근거

- **리포트 단위 = 세션에서 채점한 트레이스 전체.** trace 메타(name·timestamp)는 세션 `traces` 와 id 로 join.
- 집계: 기준별 평균(0~10), overall 평균, findings 심각도별(HIGH/MED/LOW) 건수.
- **json 은 `ensure_ascii=False`** 로 한글 보존, **md 표 셀은 파이프·개행 이스케이프**.
- `generated_at` 은 조립 시각(`datetime.now`). (앱 코드라 제약 없음)

## 검증

- 합성 `TraceScorecard` 로 build/export 로직 확인: 평균·집계·md/json 형식 정상(한글 유지).
- `import app.main` 성공(리포트 임포트 체인 포함)·py_compile OK.
- 콘솔 print 는 cp949 에서 em대시(—) 인코딩 에러가 났지만 문자열·파일 출력은 정상(콘솔 한정 이슈).

## 다음 할 일

- 라이브 GUI 에서 채점 후 리포트 섹션·다운로드 최종 확인(선택).
- (M4) 리포트 기반 프롬프트 개선 제안 + 테스트 데이터 생성.
